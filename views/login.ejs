<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css">
    <title>Iniciar Sesión - Hotel Flamingo</title>
</head>

<body>
    <section class="auth-layout">
        <div class="card">
            <div class="form-wrap">
                <!-- Logo/Marca -->
                <div style="text-align: center; margin-bottom: 28px;">
                    <h1 class="auth-brand" style="margin-bottom: 8px;">🏨 Hotel Flamingo</h1>
                    <p class="form-sub" style="margin: 0;">Tu hotel de confianza para hospedarte</p>
                </div>

                <h2 class="form-title">Iniciar Sesión</h2>
                <p class="form-sub">Accede a tu cuenta para gestionar tus reservas</p>

                <form action="/auth/login" method="POST" class="form">
                    <div>
                        <label for="email" class="label">📧 Correo Electrónico</label>
                        <input 
                            type="email" 
                            id="email" 
                            name="email" 
                            class="input" 
                            placeholder="tu@email.com" 
                            required
                            autocomplete="email"
                        >
                    </div>

                    <div>
                        <label for="contrasena" class="label">🔐 Contraseña</label>
                        <input 
                            type="password" 
                            id="contrasena" 
                            name="contrasena" 
                            class="input" 
                            placeholder="••••••••" 
                            required
                            autocomplete="current-password"
                        >
                    </div>

                    <div class="form-actions">
                        <button type="submit" class="btn btn-primary">Iniciar Sesión</button>
                    </div>
                </form>

                <!-- Mensajes de error o éxito -->
                <% if (typeof error !=='undefined' && error) { %>
                    <div class="error mt-16">
                        ⚠️ <%= error %>
                    </div>
                <% } %>

                <% if (typeof mensaje !=='undefined' && mensaje) { %>
                    <div class="success mt-16">
                        ✓ <%= mensaje %>
                    </div>
                <% } %>

                <!-- Link a registro -->
                <div style="text-align: center; margin-top: 24px; padding-top: 24px; border-top: 1px solid var(--border);">
                    <p class="help" style="margin: 0 0 8px 0;">¿No tienes una cuenta?</p>
                    <a href="/auth/registro" style="font-size: 1.05rem;">Crea tu cuenta ahora</a>
                </div>

                <!-- Botón Google OAuth -->
                <div style="text-align: center; margin-top: 32px;">
                    <a href="/auth/google" class="btn btn-google" style="display: inline-flex; align-items: center; gap: 8px; background: #fff; color: #333; border: 1px solid #ccc; border-radius: 6px; padding: 10px 18px; font-weight: 600; font-size: 1.08rem; box-shadow: 0 2px 8px rgba(0,0,0,0.04); transition: box-shadow .2s;">
                        <img src="https://upload.wikimedia.org/wikipedia/commons/5/53/Google_%22G%22_Logo.svg" alt="Google" style="width: 22px; height: 22px; vertical-align: middle;"> Iniciar sesión con Google
                    </a>
                </div>
            </div>
        </div>
    </section>
</body>

</html>